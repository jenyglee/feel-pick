# compiled output (모노레포: 모든 워크스페이스의 산출물/모듈/커버리지)
dist/
node_modules/
coverage/
/build

# Turborepo 로컬 캐시
.turbo

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# temp directory
.temp
.tmp

# 업로드된 사용자 파일(프로필 사진). 로컬 디스크 저장 — 커밋 대상 아님.
# 경로를 고정한다 — `uploads/`로 두면 소스 폴더(src/uploads/)까지 무시된다.
/apps/backend/uploads/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Claude Code (개인 설정만 제외, .claude/settings.json은 팀 공유 가능)
.claude/settings.local.json

/generated/prisma
